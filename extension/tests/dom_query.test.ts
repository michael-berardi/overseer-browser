import { it, expect } from 'vitest';
import { queryDom, validateDomQuery } from '../src/dom_query';
import { runInNewContext } from 'node:vm';
const fixture = "\nclass Element {\n constructor(tag, attrs={}, children=[]) {\n  this.localName=tag; this.attrs=attrs; this.children=children; this.isConnected=true;\n  this.type=attrs.type || 'text'; this.labels=[]; this.readOnly=false;\n  if ('value' in attrs) this.value=attrs.value;\n  for(let i=0;i<children.length;i++) { children[i].parentElement=this; children[i].previousElementSibling=children[i-1] || null; }\n }\n get lastElementChild(){return this.children.at(-1) || null;}\n getAttribute(k){return this.attrs[k] ?? null;}\n hasAttribute(k){return k in this.attrs;}\n getRootNode(){return document;}\n matches(s){if(s==='[')throw Error(); if(s===':disabled')return this.hasAttribute('disabled'); return s==='*'||s===this.localName||(s[0]==='#' && this.attrs.id===s.slice(1));}\n getBoundingClientRect(){return {x:0,y:0,width:10,height:20};}\n getClientRects(){return [this.getBoundingClientRect()];}\n}\nconst document={getElementById:()=>null,createTreeWalker(e){let done=false;return {nextNode(){if(done)return null;done=true;return e.attrs.text===undefined?null:{data:e.attrs.text};}};}};\nconst getComputedStyle=e=>({visibility:'visible',display:'block',opacity:'1',getPropertyValue:n=>n==='color'?'rgb(0, 0, 0)':''});\nconst a=new Element('input',{id:'a',value:'',text:'Alpha','data-testid':'x'});\nconst b=new Element('input',{id:'b',value:'0',text:'Beta','data-testid':'x'});\na.checked=false;\nconst host=new Element('widget');\nconst shadow=new Element('button',{'data-testid':'shadow',text:'Shadow'});\nhost.shadowRoot={lastElementChild:shadow};\nshadow.getRootNode=()=>({host});\ndocument.documentElement=new Element('html',{},[a,b,host]);\n";
it('canonical injected query 0', () => { const p = validateDomQuery({"action": "get", "query": "count", "locator": {"css": "absent"}, "names": []}); const result = runInNewContext(fixture + '\n(' + queryDom.toString() + ')(p)', {p, performance, TextEncoder}); expect(result.ok).toBe(true); expect(result.result).toEqual(0); });
it('canonical injected query 1', () => { const p = validateDomQuery({"action": "is", "query": "checked", "locator": {"css": "#a"}, "names": []}); const result = runInNewContext(fixture + '\n(' + queryDom.toString() + ')(p)', {p, performance, TextEncoder}); expect(result.ok).toBe(true); expect(result.result).toEqual(false); });
it('canonical injected query 2', () => { const p = validateDomQuery({"action": "get", "query": "value", "locator": {"css": "input", "last": true}, "names": []}); const result = runInNewContext(fixture + '\n(' + queryDom.toString() + ')(p)', {p, performance, TextEncoder}); expect(result.ok).toBe(true); expect(result.result).toEqual("0"); });
it('canonical injected query 3', () => { const p = validateDomQuery({"action": "get", "query": "count", "locator": {"testid": "shadow"}, "names": []}); const result = runInNewContext(fixture + '\n(' + queryDom.toString() + ')(p)', {p, performance, TextEncoder}); expect(result.ok).toBe(true); expect(result.result).toEqual(1); });
it('canonical injected query 4', () => { const p = validateDomQuery({"action": "get", "query": "text", "locator": {"css": "#a"}, "names": []}); const result = runInNewContext(fixture + '\n(' + queryDom.toString() + ')(p)', {p, performance, TextEncoder}); expect(result.ok).toBe(true); expect(result.result).toEqual("Alpha"); });
it('rejects executable and unbounded data', () => { for (const p of [{source:'alert(1)'}, {action:'find',query:null,locator:{css:'a',frame:1},names:[]}, {action:'find',query:null,locator:{css:'a',nth:true},names:[]}]) expect(() => validateDomQuery(p)).toThrow(); });
